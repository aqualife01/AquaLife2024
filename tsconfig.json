{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "typeRoots": ["./node_modules/@types"],
    "baseUrl": "./", // Agrega esto
    "paths": {
      "*": ["src/*"] // Opcional: si tienes todos tus archivos dentro de `src`
    }
  }
}
